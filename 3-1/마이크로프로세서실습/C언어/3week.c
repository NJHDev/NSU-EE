/* 3-1 사칙연산자 계산
#include <stdio.h>

int main() {
    int input = 100, input2 = 5, sum = 0, sub = 0, multi = 0, divid = 0, mod = 0;

    sum = input + input2;
    sub = input - input2;
    multi = input * input2;
    divid = input / input2;
    mod = input % input2;

    printf("덧셈결과 = %d\n", sum);
    printf("뺄셈결과 = %d\n", sub);
    printf("곱셈결과 = %d\n", multi);
    printf("나눗셈결과 = %d\n", divid);
    printf("모듈로결과 = %d\n", mod);
    
    return 0;
    
}
*/

/* 3-2 비교연산자 계산
