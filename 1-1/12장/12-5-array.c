/*
// array.c : 함수 정의는 소스 파일에 넣어준다.
#include <stdio.h>      //  라이브러리를 사용하는 소스 파일마다 헤더를 포함한다.
#include "12-5-array.h"      // 자기 자신의 헤더 파일을 포함하는 것이 좋다.

void print_array(const int arr[], int size)
{
    int i;
    for (i = 0; i < size; i++)
        printf("%d ", arr[i]);
    printf("\n");
}

void fill_array(int arr[], int value, int size)
{
    int i;
    for (i = 0; i < size; i++)
        arr[i] = value;
}
*/