/*
// main.c : main 함수는 별도의 소스 파일에 작성하는 것이 좋다.
#include <stdio.h>      //  라이브러리를 사용하는 소스 파일마다 헤더를 포함한다.
#include "12-5-array.h"      // array.c에 정의된 함수를 호출하려면 array.h를 포함한다.

void main()
{
    int data[10];

    fill_array(data, 1, 10);        // data 배열 전체에 1을 채운다.
    print_array(data, 10);          // data 배열을 출력한다.
}
*/