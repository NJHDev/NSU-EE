/*
#include <stdio.h>

int get_max(int x, int y, int z)
{
    int max = x > y ? x : y;
    max = z > max ? z : max;
    return max;
}

void main()
{
    //get_max(10, 20);
    //get_max(10, 20, 30, 40);

    double max;
    max = get_max(12.34, 0.5, 7.9);
    printf("max = %f\n", max);

    //get_ma(1, 2, 3);
}
*/