/*
#include <stdio.h>

void main()
{
    int num = 0, sum = 0;
    
    while (1) 
	{
        printf("정수?(음수 입력시 종료) ");
        scanf("%d", &num);
        if (num < 0)
            break;
        if (num % 2 == 0)
            continue;
        sum += num;
    }
    printf("홀수의 합계: %d\n", sum);
}
*/