/*
#include <stdio.h>

void main()
{
	int num = 0, sum = 0, i, n;
	
	printf("입력할 정수의 갯수? ");
	scanf("%d", &n);
	printf("정수 %d개를 입력하세요: ", n);
	for (i=0; i<5; i++) 
	{
		scanf("%d", &num);
		sum += num;           //sum = sum + num;
	}
	printf("합계: %d\n", sum);
}
*/