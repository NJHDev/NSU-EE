/*#include <stdio.h>

void main()
{
    int x, y;
    double ave;

    printf("Input two numbers: ");
    scanf("%d %d", &x, &y);

    ave = (x + y) / 2;
    printf("averate = %f\n", ave);
    ave = (double) (x + y) / 2;
    printf("average = %f\n", ave);   
}
*/