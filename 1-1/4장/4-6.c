/*
#include <stdio.h>

void main()
{
    int usage;

    printf("usage? ");
    scanf("%d", &usage);

    if (usage >= 1000 && usage <= 2000)
        printf("usage in range\n");

    if (usage < 1000 || usage > 2000)
        printf("out of range\n");
}
*/