/*
#include <stdio.h>

void main()
{
	int i,per,kor[1000], eng[1000], math[1000];
	char name[1000],line;
	float sum, avr;

	printf("학생수 입력:");
	scanf("%d",&per);

	for (i=0; i<=per; i++)
		{
			printf("%d번 학생 이름 입력 : ", i+1);
			scanf("%s",name[i]);

			printf("%d 학생 국어, 영어, 수학 성적 입력 : ", i);
			scanf("%d %d %d \n",kor[i],eng[i],math[i]);
		}

	printf("==========================\n\n");

}
*/