/*
#include <stdio.h>

void main()
{
    int arr[5];
    int size = 0;
    int i;

    printf("배열의 바이트 크기: %d\n", sizeof(arr));

    size = sizeof(arr) / sizeof(arr[0]);
    printf("배열의 크기: %d\n", size);

    for (i = 0; i < size; i++)
        arr[i] = 0;

    for (i = 0; i < size; i++)
        printf("%d ", arr[i]);
    printf("\n");
}
*/